# ssh id setup in bith the local macheine and the remote 
# add commit 

# terminal : remote -v 
# remote add origin <link of github repo>
linking the remote location with local machiene



A remote is a GitHub repository connected to your local repository.
origin is simply a nickname for a remote repository.

# when you clone a repo
git clone https://github.com/user/project.git
# Git automatically creates:
origin
# Example:
origin
↓
https://github.com/user/project.git
# Check:
git remote -v
# Output:
origin  https://github.com/user/project.git (fetch)
origin  https://github.com/user/project.git (push)


upstream is usually the original repository from which you forked
