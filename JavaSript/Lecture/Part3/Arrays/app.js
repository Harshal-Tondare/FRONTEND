let cars = ["audi", "bmw", "maruti", "xuv"];
let newArr = []; // empty array
// arr.length;

// A R R A Y     M E T H O D S
 // arrays are object in javascript

// cars.push("toyota");
// cars.pop(); 
// cars.unshift("thar")--> add to start
// shift("audi"). --> delete from start & return it


//.indexof("something"); -->returns index of something
//.includes("something"); -->search for value -->true/false

//  pri.concat(sec) --> merge 2 arrays pri & sec
//  arr.reverse(); --> reverse an array(Orignal array changes)



// slice method ==> copies a portion of an array
// arr.slice();--> copy of origial array
// arr.slice(x); ---> x idx start se last tak
// arr.slice(x,y); ---> (start,end) eg(2,4) 2--3(ending idx not included)
// arr.slice(-x); --> last se x elements


// splice : remove / replaces / add elements in place
// splice( start, deleCount, item0 .... itemN)
// .splice(1,3) 1st idx se 3 elemet ko delete kar do

// sort method-->. .sort();
// array of number sort() make number into string internally then sort it
//  so better to use this method when you have char element or string element in array for number it not useful



// Array refrence --> address of an array

// const arr= []; elements can be changed but we can't convert arr completely into new array

// Nested arrays
// let nums = [[1,2],[3,4],[5,6]]; // multidimensional Array
