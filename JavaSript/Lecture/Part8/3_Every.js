// return true if every element of array gives true for some function else return false;
let arr = [1,2,3,4];
arr.every( (el) => (el%2==0));
// some function
//Retuurn true if some element of array true for some function.Else returns false
arr.some((el) =>{el%2==0});